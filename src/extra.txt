x = raw.pick(picks = Configuration.channelList()).get_data()[:,1:102400+1]
y = finalEmgData2.transpose(0,1,2).reshape(16,-1)
singleValue = []
for i in range(x.shape[0]):
    for j in range(x.shape[1]):
        if x[i,j] == y[i,j]:
            singleValue.append(x[i,j]) 

data_points = np.array(singleValue)
data = data_points.transpose(0).reshape(16,-1)
data = data/1e-6
data.shape
